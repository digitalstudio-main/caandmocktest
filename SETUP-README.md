# DIGITAL STUDIO — সেটআপ গাইড

## ফাইলগুলো কী কী
- `index.html` — মূল ওয়েবসাইট (লগইন, রেজিস্টার, হোম, মেনু, সব মডাল)
- `style.css` — ডিজাইন
- `config.js` — **শুধু এই ফাইলে আপনার ব্যাকএন্ড URL বসাতে হবে**
- `language.js` — English/বাংলা/हिन्दी অনুবাদ
- `auth.js` — লগইন/রেজিস্ট্রেশন লজিক
- `admin.js` — অ্যাডমিন প্যানেল, ইউজার প্রোফাইল ম্যানেজমেন্ট
- `current-affairs.js` — Daily Current Affairs ফিচার
- `mocktest.js` — Mock Test তৈরি, দেওয়া, রেজাল্ট
- `apps-script-backend.gs` — Google Apps Script ব্যাকএন্ড কোড (ডাটাবেস)
- `logo.png` — আপনার লোগো

সব ফাইল **একই ফোল্ডারে** রাখবেন (হোস্টিং এ আপলোড করার সময়ও একসাথে)।

---

## ধাপ ১ — Google Sheet + Apps Script ব্যাকএন্ড বানানো

1. [sheets.google.com](https://sheets.google.com) এ গিয়ে একটা নতুন ফাঁকা Spreadsheet বানান, নাম দিন যেমন "Digital Studio DB"।
2. মেনু থেকে **Extensions > Apps Script** এ ক্লিক করুন।
3. যে এডিটর খুলবে, সেখানে যা লেখা আছে সব মুছে `apps-script-backend.gs` ফাইলের পুরো কোডটা পেস্ট করুন।
4. উপরে **Save** (💾) করুন।
5. **Deploy > New deployment** এ ক্লিক করুন।
   - Type হিসেবে **Web app** বেছে নিন (গিয়ার আইকনে ক্লিক করে)।
   - Execute as: **Me**
   - Who has access: **Anyone**
6. **Deploy** এ ক্লিক করুন। প্রথমবার পারমিশন চাইবে — Google account দিয়ে Authorize করুন (Advanced > Go to project (unsafe) লেখা এলে ভয় পাবেন না, এটা আপনার নিজের স্ক্রিপ্ট বলেই আসে)।
7. যে **Web app URL** পাবেন (অনেকটা `https://script.google.com/macros/s/AKfycb.../exec` এরকম দেখতে), সেটা কপি করুন।

এই স্প্রেডশিটেই স্বয়ংক্রিয়ভাবে ৪টা শিট তৈরি হয়ে যাবে যখন প্রথমবার কেউ ব্যবহার করবে: `Users`, `CurrentAffairs`, `Quizzes`, `Results`।

## ধাপ ২ — config.js এ URL বসানো

`config.js` ফাইল খুলে এই লাইনটা খুঁজুন:
```js
APPS_SCRIPT_URL: "PASTE_YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL_HERE",
```
এখানে ধাপ ১ থেকে পাওয়া URL টা বসিয়ে সেভ করুন। ব্যাস, ওয়েবসাইট এখন থেকে আসল ডেটাবেসে সেভ করবে।

> কোড কখনো আপডেট করলে (Apps Script এডিটরে গিয়ে) — **Deploy > Manage deployments > Edit (✏️) > New version > Deploy** করতে হবে, নাহলে পুরনো কোডই চলতে থাকবে।

## ধাপ ৩ — ওয়েবসাইট হোস্ট করা

`index.html` সহ সব ফাইল যেকোনো ফ্রি হোস্টিং এ আপলোড করলেই চলবে, যেমন:
- **GitHub Pages** (ফ্রি, নিজের ডোমেইনও যোগ করা যায়)
- **Netlify** / **Vercel** (ফ্রি, ড্র্যাগ-অ্যান্ড-ড্রপ আপলোড)
- আপনার আগের মতো কোনো হোস্টিং থাকলে সেখানেও ফোল্ডার আপলোড করলেই চলবে।

শুধু মনে রাখবেন — সব ফাইল (html, css, js, png) একই ফোল্ডারে থাকতে হবে।

## Firebase (ঐচ্ছিক, পরে করলেও চলবে)

আপাতত Google Sheets দিয়েই লগইন, রেজিস্ট্রেশন, কারেন্ট অ্যাফেয়ার্স, মক টেস্ট সব কাজ করবে। ভবিষ্যতে যদি:
- ছবি আপলোড (এখন প্রশ্ন/অপশনে শুধু ছবির **লিংক** বসানো যায়, ফাইল আপলোড না)
- রিয়েল-টাইম আপডেট (কেউ কিছু যোগ করলেই সবার স্ক্রিনে সাথে সাথে দেখানো)

— এগুলো দরকার হয়, তখন Firebase যোগ করা যাবে। `config.js` এ `FIREBASE_CONFIG` এর জায়গা রেখে দেওয়া হয়েছে, শুধু বলবেন তখন আমি বাকি কোড জুড়ে দেব।

---

## এখন যা যা কাজ করছে
- রেজিস্ট্রেশন (ফোন ১১ ডিজিট চেক, ইমেইল ফরম্যাট চেক, ইউনিক ফোন/ইমেইল চেক, পাসওয়ার্ড ম্যাচ চেক)
- লগইন (Gmail বা ফোন দিয়ে)
- অ্যাডমিন লগইন (Login ID: `Digital Studio`, Password: `Joy@732122` — `config.js` থেকে বদলানো যাবে)
- হোম পেজ — লোগো, ফেক স্ট্যাটস, ভাষা পরিবর্তন (English/বাংলা/हिन्दी)
- তিন-ডট মেনু — Daily Current Affairs, Mock Tests কাজ করছে; বাকিগুলোতে "Coming soon" দেখায়
- Daily Current Affairs — অ্যাডমিন ফাইল যোগ করতে পারবে, স্টুডেন্ট ক্লিক করে পড়তে পারবে
- Mock Tests — সবার জন্য ফ্রি, আজ থেকেই চালু। অ্যাডমিন কুইজ বানাবে, প্রশ্ন যোগ করবে (৪ অপশন, ছবি লিংক, সঠিক উত্তর, positive/negative মার্কস, ব্যাখ্যা), স্টুডেন্ট টেস্ট দেবে — টাইমার শেষ হলে অটো-সাবমিট, স্কোর স্বয়ংক্রিয়ভাবে হিসাব হয়ে যাবে। অ্যাডমিন প্রতিটা স্টুডেন্টের ফলাফল (স্কোর, সঠিক/ভুল, উত্তর, সময়) দেখতে পারবে
- User Profiles (অ্যাডমিন) — সবার প্রোফাইল দেখা (Access), Block/Unblock, Delete — প্রতিটাতে "Are you sure?" কনফার্মেশন

## এখনো যা বাকি / সীমাবদ্ধতা
- WBCS/WBPSC/Grampanchayat/SSC/D.El.Ed/TET/CTET/Job/Scholarship — এগুলো এখনো "Not available" দেখায়, যেমনটা আপনি বলেছিলেন
- ছবি এখন শুধু লিংক দিয়ে যোগ করা যায় (গ্যালারি থেকে সরাসরি আপলোড করতে Firebase Storage লাগবে — পরে যোগ করা যাবে)
- হোম পেজের লেখা/স্ট্যাটস এখনো কোড ফাইলেই আছে (আপনি যেমন চেয়েছিলেন — অ্যাডমিন প্যানেল থেকে এডিট করা যাবে না)
